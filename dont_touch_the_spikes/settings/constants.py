import pygame

START = 'START'
GAME = 'GAME'
WIDTH = 400
HEIGHT = 700
CENTER = (WIDTH / 2, HEIGHT / 2)
PLAYER_SIZE = (int(712 / 10), int(520 / 10))
BACKGROUND = pygame.image.load("assets/images/background/background.png")
